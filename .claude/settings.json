{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; echo \"$f\" | grep -q 'examples/nextjs/src/' && cd /Users/mks/Desktop/C0D3/pizow/examples/nextjs && npx tsc --noEmit --skipLibCheck 2>&1 || true; }",
            "timeout": 30,
            "statusMessage": "Type-checking..."
          }
        ]
      }
    ]
  }
}
